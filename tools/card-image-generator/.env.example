# Optional local settings. No API key, account password, or token required.
# Zero disables the local daily cap. ChatGPT plan limits still apply.
DAILY_GENERATION_LIMIT=0
# Optional: absolute path to dart.exe or dart.bat
# DART_EXECUTABLE=
